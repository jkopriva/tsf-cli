OpenShift Subscriptions:
{{- range $sub := include "subscriptions.managed" . | fromYaml }}
  - Subscription: "{{ $sub.name }}" ({{ $sub.namespace }})
    API-Resource: "{{ $sub.apiResource }}"
    Channel:      "{{ $sub.channel }}"
{{- end }}
